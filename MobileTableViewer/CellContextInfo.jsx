import React from 'react';

/**
 * CellContextInfo Component
 * 
 * Displays contextual information about the current cell, such as row and column labels.
 * This component's functionality has been integrated into the main MobileTableViewer component
 * with better styling and responsive layout.
 * 
 * Component kept for backward compatibility but doesn't render anything.
 */
const CellContextInfo = ({ currentCellData, theme }) => {
  return null;
};

export default CellContextInfo;
